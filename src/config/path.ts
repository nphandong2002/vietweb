const root = {
  home: '/',
  room: '/room',
  login: '/login',
  register: '/register',
  setting: '/setting',
};

export const PATHS = {
  home: { root: root.home },
  setting: { root: root.setting },
  room: { root: root.room },
  login: { root: root.login },
  register: { root: root.register },
};
