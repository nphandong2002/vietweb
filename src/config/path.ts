const root = {
  home: '/',
  room: '/room',
  login: '/login',
  register: '/register',
};

export const PATHS = {
  home: { root: root.home },
  room: { root: root.room },
  login: { root: root.login },
  register: { root: root.register },
};
