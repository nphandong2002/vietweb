"use client";
import { useEffect, useRef } from "react";

function GamePage() {
  return (
    <canvas
      style={{
        width: "100%",
        height: "100%",
      }}
    ></canvas>
  );
}

export default GamePage;
